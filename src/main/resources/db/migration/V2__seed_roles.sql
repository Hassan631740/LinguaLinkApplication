-- V2__seed_roles.sql: Roles are defined as enum in code (Role.java)
-- No database seeding needed since roles are static enum values:
-- - CLIENT
-- - INTERPRETER  
-- - ADMINISTRATOR
-- This migration file is kept for versioning consistency.

